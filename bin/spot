#!/usr/bin/env python

import argparse
from spot import __version__, runner


def list_command(args):
    for r in runner.list_all():
        print(r)


def run_command(args):
    r = runner.load(args.runner)
    facts = r.execute(dict(args.parameters))

    if args.submit:
        from spot.client import Spot
        client = Spot()
        client.submit_facts(r, facts)


def submit_command(args):
    from spot.client import Spot

    result = runner.load(args.runner)
    schema = runner.RunnerSchema()
    client = Spot()
    client.submit_runner(schema.dumps(result).data)


def key_value_assignment(arg):
    t = tuple(arg.split('='))

    if len(t) != 2:
        raise argparse.ArgumentTypeError("`{}' has no value".format(t[0]))

    if t[1] == '':
        raise argparse.ArgumentTypeError("Value of `{}' is empty".format(t[0]))

    return t


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument('--version',
                        action='version',
                        version='spot v{}'.format(__version__))

    command_parsers = parser.add_subparsers(title="Commands")

    list_parser = command_parsers.add_parser('list', help="List all runners")
    list_parser.set_defaults(run=list_command)

    run_parser = command_parsers.add_parser('run', help="Execute a runner")
    run_parser.set_defaults(run=run_command)

    run_parser.add_argument('--submit', action='store_true', default=False)
    run_parser.add_argument('runner', metavar='RUNNER', type=str)
    run_parser.add_argument('parameters', metavar='KEY=VAL', type=key_value_assignment, nargs='*')

    submit_parser = command_parsers.add_parser('submit', help="Submit data to server")
    submit_parser.set_defaults(run=submit_command)

    submit_parser.add_argument('runner', metavar='RUNNER', type=str)

    args = parser.parse_args()

    try:
        args.run(args)
    except (runner.LoadError, runner.ExecutionError) as e:
        print("Error: {}".format(e))


if __name__ == '__main__':
    main()
